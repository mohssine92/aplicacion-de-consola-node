# notas 


```
node app --help
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     recibe numero al que quieres generar su archivo.txt  con tabla
                 de  multiplicacion                          [number] [required]
  -l, --listar    listar en consola el resultado de tabla multiplicaion del
                  archivo que se va generar en este caso
                                                      [boolean] [default: false]
  -h, --hasta     multiplicar Hasta ... recibe numero , si no se declara su
                  valor se va por defecto es 10                    [default: 10]
```